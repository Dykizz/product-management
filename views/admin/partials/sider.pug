.sider 
    .inner-menu 
        ul 
            li 
                a(href=`${prefixAdmin}/dashboard`) Tổng quan
            if role.permissions.includes('product_detail')
                li 
                    a(href=`${prefixAdmin}/products`) Danh sách sản phẩm
            if role.permissions.includes('category_detail')
                li 
                    a(href=`${prefixAdmin}/categories`) Danh mục sản phẩm
            if role.permissions.includes('role_detail')
                li 
                    a(href=`${prefixAdmin}/roles`) Nhóm quyền
            if role.permissions.includes('role_permission')
                li 
                    a(href=`${prefixAdmin}/roles/permission`) Phân quyền
            if role.permissions.includes('account_detail')
                li 
                    a(href=`${prefixAdmin}/accounts`) Danh sách tài khoản
            if role.permissions.includes('setting-general_detail')
                li  
                    a(href=`${prefixAdmin}/setting/general`) Cài đặt chung