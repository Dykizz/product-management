extends ../../layouts/default.pug
include ../../mixins/notification.pug 
block main 
    +notification(4000)
    h2.mb-3 Chi tiết nhóm quyền 
    .card.mb-3
        .card-header 
            h3 #{role.title}
        .card-body Mô tả chi tiết : 
            | #{role.description}
    a(
        class="btn btn-warning"
        href=`${prefixAdmin}/roles/edit/${role._id}`
    ) Sửa thông tin