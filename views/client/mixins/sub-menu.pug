mixin sub-menu(tree, path, lv)
    ul(class=`dropdown${lv < 3 ? lv : 2}`)
        each item in tree
            li
                a(href=`${path}/${item.slug}`) #{item.title}
                if item.children && item.children.length > 0
                    +sub-menu(item.children, path, lv + 1)
